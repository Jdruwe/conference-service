image: gradle:jre11-slim

stages:
    - build

build:
    stage: build
    script:
        - ./gradlew clean build
        - ./gradlew pmdMain
        - ./gradlew pmdTest
    artifacts:
        paths:
            - build/libs/*.jar
